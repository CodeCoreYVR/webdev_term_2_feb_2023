<h2>All questions</h2>
<% @questions.each do |question| %>
    <div>
        <h3><%= link_to(question.title,question_path(question)) %> </h3>
        <p>
        <%= question.body %>
        </p>
    </div>
<% end %>