# Define the HelperMethods module in a separate file
module HelperMethods
  # Define a method that returns a random number between 0 and the input parameter 'max'
  def random_number(max)
    rand(max)
  end
end